"use client";
import { FormEvent, useState } from "react";
export default function ContactForm() { const [sent, setSent] = useState(false); const submit = (e: FormEvent<HTMLFormElement>) => { e.preventDefault(); if (!e.currentTarget.checkValidity()) { e.currentTarget.reportValidity(); return; } setSent(true); e.currentTarget.reset(); }; return <form onSubmit={submit} className="content-card p-6 sm:p-8"><h2 className="text-2xl font-black text-[var(--dark-navy)]">Send an enquiry</h2><div className="mt-6 grid gap-5 sm:grid-cols-2"><label className="form-label">Name *<input required className="form-control" /></label><label className="form-label">Phone *<input required type="tel" className="form-control" /></label><label className="form-label sm:col-span-2">Email *<input required type="email" className="form-control" /></label><label className="form-label sm:col-span-2">Message *<textarea required rows={5} className="form-control" /></label></div>{sent && <p role="status" className="mt-5 rounded-xl bg-green-50 p-4 font-bold text-green-800">Thank you. DS CREATIONS will contact you to discuss your enquiry.</p>}<button type="submit" className="btn-gold mt-6">Send Enquiry</button></form>; }
