"use client";
import { FormEvent, useState } from "react";
import { products } from "@/data/products";

export default function BulkOrderForm() { const [sent, setSent] = useState(false); const submit = (e: FormEvent<HTMLFormElement>) => { e.preventDefault(); if (!e.currentTarget.checkValidity()) { e.currentTarget.reportValidity(); return; } setSent(true); e.currentTarget.reset(); }; const control = "form-control"; return <form onSubmit={submit} className="content-card p-6 sm:p-10"><div className="grid gap-5 md:grid-cols-2"><label className="form-label">Customer name *<input required className={control} /></label><label className="form-label">School, company or institution *<input required className={control} /></label><label className="form-label">Phone *<input required type="tel" className={control} /></label><label className="form-label">Email *<input required type="email" className={control} /></label><label className="form-label">Product *<select required className={control} defaultValue=""><option value="" disabled>Select a product</option>{products.map((p) => <option key={p.id}>{p.name}</option>)}<option>Other requirement</option></select></label><label className="form-label">Quantity *<input required min="1" type="number" className={control} /></label><label className="form-label">Sizes<input className={control} placeholder="e.g. S–2XL or size-wise breakup" /></label><label className="form-label">Colours<input className={control} placeholder="Preferred colours" /></label>{["Custom logo requirement", "Printing requirement", "Embroidery requirement"].map((label) => <label className="form-label" key={label}>{label}<select className={control}><option>No</option><option>Yes</option><option>Need guidance</option></select></label>)}<label className="form-label">Delivery location *<input required className={control} /></label><label className="form-label">Expected delivery date<input type="date" className={control} /></label><label className="form-label md:col-span-2">Additional details<textarea rows={5} className={control} /></label></div>{sent && <p role="status" className="mt-6 rounded-xl bg-green-50 p-4 font-bold text-green-800">Thank you. Your Phase 1 enquiry has been prepared. The DS CREATIONS team will contact you to confirm the requirement.</p>}<button type="submit" className="btn-gold mt-7 px-8">Submit Bulk Enquiry</button><p className="mt-4 text-sm text-slate-500">Frontend-only prototype; no database submission is made.</p>{/* TODO Phase 3: Store validated enquiries in the database and expose them in the admin workflow. */}</form>; }
